import { DashboardLoading } from '../../../components/dashboard-loading';

export default function NotificationsLoading() {
  return (
    <DashboardLoading currentSection="notifications" layout="notifications" />
  );
}
