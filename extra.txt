okay so the next issue i want you to work on is the agent configuration page,
in the agent behvior section, when i type a greeting message, like a custom one, it does not speak using that information
and same issue is with the special instruction and the fallback message, i want the configuration to exactly reflect in the agent session when i click connect butotn in the test agent section.


the filter section in the conversationpage does not work, it does not filter out the conversations according to the filter Selected


the summary section in the converation history tab does not show the exact summary, like what i want is a simple llm call that uses te transcript and generates a summary and displays it in the UI, right now, it displays the hardcoded information


in the business configuration page